module.exports = {
    purge: ['./src/**/*.js}', './public/index.html'],
    darkMode: false, // or 'media' or 'class'
    theme: {
        fontFamily: {
            sans: ['Roboto', 'system-ui'],
            serif: ['Roboto Slab'],
        },
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
